apiVersion: v1
kind: Service
metadata:
  name: {{ include "inu-dining-backend.fullname" . }}
  labels:
    {{- include "inu-dining-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inu-dining-backend.selectorLabels" . | nindent 4 }}
