apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inu-dining-backend.fullname" . }}-test-connection"
  labels:
    {{- include "inu-dining-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inu-dining-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
